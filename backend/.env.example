# Backend environment overrides (copy to backend/.env or repo root .env)
# ===========================================================================
# At least ONE LLM API key is required for AI-powered design generation.
# If neither is provided, the system uses deterministic defaults (heuristic).
# ===========================================================================

# Gemini API (Primary - recommended)
GEMINI_API_KEY=your_gemini_key
GEMINI_AUTH_MODE=auto
GEMINI_MODEL=gemini-1.5-pro

# OpenAI API (Alternative)
OPENAI_API_KEY=sk-your_openai_key
OPENAI_MODEL=gpt-4o

# Gemini Design MCP (frontend generation)
GEMINI_DESIGN_API_KEY=your_gemini_design_key
GEMINI_DESIGN_MCP_URL=https://gemini-design-mcp-server-production.up.railway.app/mcp

# Optional overrides
# DATABASE_URL=sqlite:///./app.db
# CORS_ORIGINS=*
# STORAGE_DIR=/absolute/path/to/storage

# Optional external tools (leave empty if not installed)
# ENERGYPLUS_PATH=/path/to/energyplus
# BLENDER_PATH=/path/to/blender
